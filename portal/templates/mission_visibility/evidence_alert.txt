{{ customer }},

New evidence from your mission with Level 1 Transport, load {{ load_number }}:

{% for label in labels %}  - {{ label }}s
{% endfor %}
{% if portal_url %}{{ count }} photo{{ 's' if count != 1 else '' }} {{ 'were' if count != 1 else 'was' }} added to your Mission Visibility View. You cannot stand at the dock with us, so we show you the evidence.

See them in the Customer Portal: {{ portal_url }}
Sign in with your load number.
{% else %}{{ count }} photo{{ 's' if count != 1 else '' }} {{ 'were' if count != 1 else 'was' }} taken and {{ 'are' if count != 1 else 'is' }} kept with your mission record. You cannot stand at the dock with us, so we keep the evidence for you.

Reply to this email and we will send {{ 'them' if count != 1 else 'it' }}.
{% endif %}
Mike Zachary
Level 1 Transport Inc.

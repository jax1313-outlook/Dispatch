# GOVERNANCE AND DOCTRINE CANDIDATES

- **Generated by:** Claude Code
- **Generated (UTC):** 2026-08-24T00:00:00+00:00
- **Tool:** `dispatch.sandbox_survey` 1.0.0 — run `20260824T000000Z`
- **Sandbox path read:** `D:\Sandbox\Play Pen`
- **Output path:** `D:\Sandbox\Play Pen\Dispatch`
- **Survey status:** `ABSENT`

> **READ-ONLY PASS — NO FILES WERE MODIFIED**
>
> **Nothing in this report is accepted doctrine or a Mike decision.**

> **THIS SURVEY HAS NOT BEEN RUN.** This document is a template showing the shape of the output. No file under the Sandbox path above has been read, listed, hashed or classified.
>
> Reason: The survey was not performed. It was authored in an isolated Linux container that has no D: drive, no mount, no network path and no credential reaching the Windows machine that holds D:\Sandbox\Play Pen — that folder is ABSENT from the environment these templates were generated in. Nothing under it has been read, listed, hashed, sampled or classified. The tool's behaviour against the real Sandbox is UNVERIFIED until Mike runs it himself; see docs/readiness/SANDBOX_SURVEY_PROCEDURE.md.
>
> Every content claim in this document is `ABSENT` — meaning the step was not performed, not that the material does not exist.

---

**Notes are not doctrine.** Nothing in this document is doctrine. Material is listed here as a *candidate* because it reads like doctrine — imperative sentences, policy language, a governance filename — which is a fact about its prose style, not about its authority.

`Doctrine` as a class was assigned only where a file matches doctrine already locked in this mission or in the DISPATCH repository. Those anchors are listed below so a reader can see exactly what 'matches' meant.

## Locked doctrine anchors used for matching

| anchor | what it locks |
| --- | --- |
| TRUTH_VOCABULARY | Truth vocabulary: LIVE, CONFIGURED, UNCONFIGURED, SIMULATED, UNAVAILABLE, MANUAL, ABSENT, UNVERIFIED — these words, no synonyms. |
| NO_MANUFACTURED_ATTESTATION | An attestation of verification, approval, acceptance, authorisation or confirmation by the programme's principal is never manufactured, inferred, defaulted, auto-populated, seeded or fixtured. |
| READ_ONLY_FIRST_PASS | The first pass over the Sandbox is read-only; no file is moved, renamed, deleted, converted or overwritten. |
| TESTS_ARE_NOT_OPERATIONAL_PROOF | Repository tests are evidence of software behaviour only, never operational proof. |
| NO_SECRETS_IN_GIT | Runtime secrets, logs containing secrets, evidence files and backups are never committed to Git. |
| SOURCES_OF_TRUTH | Repositories contain implementation truth; folders contain research and knowledge evidence; Mike supplies operational truth and authority. |

## Material matching locked doctrine

`ABSENT` — the survey was not performed, so nothing is claimed here. Run the tool on the machine that holds the Sandbox (see `docs/readiness/SANDBOX_SURVEY_PROCEDURE.md`) and this section will be populated from what is actually on disk.

## Doctrine candidates

`ABSENT` — the survey was not performed, so nothing is claimed here. Run the tool on the machine that holds the Sandbox (see `docs/readiness/SANDBOX_SURVEY_PROCEDURE.md`) and this section will be populated from what is actually on disk.

## Material conflicting with locked doctrine

`ABSENT` — the survey was not performed, so nothing is claimed here. Run the tool on the machine that holds the Sandbox (see `docs/readiness/SANDBOX_SURVEY_PROCEDURE.md`) and this section will be populated from what is actually on disk.


---

_READ-ONLY PASS — NO FILES WERE MODIFIED_  
_Nothing in this report is accepted doctrine or a Mike decision._  
_No file content is quoted anywhere in this document — only paths, sizes, timestamps, hashes, class names, rule identifiers and counts._
